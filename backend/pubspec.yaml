name: tds_dart_hasura
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: ">=2.19.5 <3.0.0"

dependencies:
  args: ^2.3.0
  hasura_connect: ^4.0.0+4
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.21.0
